database:
  url: postgresql://datacontractor:datacontractor@localhost:5432/datacontractor

app:
  env: development
  debug: true

contracts:
  dir: data/contracts

datasets:
  dir: data/datasets

etl:
  block_on_critical: true
  block_on_error: false
